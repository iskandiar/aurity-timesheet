import { StyleSheet } from 'react-native'

import defaultStyles from './../../../styles/default'

export default StyleSheet.create({
  container: {
    backgroundColor: defaultStyles.monthSelectorBg,
    justifyContent: 'space-between',
    flexDirection: 'row',
    alignItems: 'center'
  },
})
